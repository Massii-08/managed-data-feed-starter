# Feedsmith environment template — copy to .env and fill in.
# Do NOT commit your real .env (it is gitignored). No secrets belong in source.

# Optional webhook URL used to deliver out-of-band alerts when a feed is
# unhealthy (consecutive failures cross the heal threshold). Leave blank to
# disable alert delivery.
FEEDSMITH_ALERT_WEBHOOK=
