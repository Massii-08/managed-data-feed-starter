# Feedsmith runtime dependencies (loose lower-bound pins).
# For development install the package instead: pip install -e '.[dev]'
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2
apscheduler>=3.10
beautifulsoup4>=4.12
httpx>=0.27
pyyaml>=6.0

# Optional: TLS impersonation fetcher (showcases the hard-site capability).
# Install via the extra instead of uncommenting here: pip install '.[impersonate]'
# curl_cffi>=0.7

# Optional: Parquet delivery sink (columnar output for data / AI pipelines).
# Install via the extra instead of uncommenting here: pip install '.[parquet]'
# pyarrow>=14
