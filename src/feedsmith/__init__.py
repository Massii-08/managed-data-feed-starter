"""Feedsmith — Managed Data Feed starter package.

Feedsmith scrapes a public source on a schedule, enforces a no-PII field
policy in code, monitors feed health with self-healing, and delivers clean
factual data as CSV, JSON, or webhook payloads.

This package uses a src-layout and is installed via ``pip install -e .`` so
modules import as ``from feedsmith.X import Y``.
"""
from __future__ import annotations

__version__ = "0.1.0"
